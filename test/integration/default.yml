- hosts: localhost
  roles:
    - ansible-role-editors
  environment:
    PKG_PATH: "{% if ansible_os_family == OpnBSD %}http://ftp.openbsd.org/pub/OpenBSD/{{ ansible_distribution_release }}/packages/{{ ansible_architecture }}/{% endif %}"
  vars:
